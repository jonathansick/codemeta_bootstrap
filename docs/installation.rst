============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install codemeta_bootstrap
    $ pip install codemeta_bootstrap

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv codemeta_bootstrap
    $ pip install codemeta_bootstrap
